// Requires
const fs = require('fs')
const junk = require('junk')

// Properties
const commitDiretorio = './commit/'
const banco_temporario_diretorio = './banco_temporario/banco_temporario.json'

// Module
module.exports = ( () => { 
    return { 
        post: ( request, response ) => {

        }
    }
})()